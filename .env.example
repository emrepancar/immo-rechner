# Server
PORT=3001
NODE_ENV=development

# Database path (defaults to ./properties.db in development)
# DB_PATH=/app/data/properties.db

# Frontend — leave empty in development (Vite proxy handles it)
# Set to the full server URL only if running frontend separately from backend
# VITE_API_URL=http://localhost:3001
